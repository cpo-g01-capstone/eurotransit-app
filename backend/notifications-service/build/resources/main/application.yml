server:
  port: 8085
spring:
  application:
    name: notifications-service
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      probes:
        enabled: true
