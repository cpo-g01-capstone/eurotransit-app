server:
  port: 8085
  shutdown: graceful
spring:
  lifecycle:
    timeout-per-shutdown-phase: 50s
  application:
    name: notifications-service
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      probes:
        enabled: true
