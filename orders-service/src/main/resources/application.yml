server:
  port: 8082
spring:
  application:
    name: orders-service
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      probes:
        enabled: true
