server:
  port: 8084
spring:
  application:
    name: payments-service
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      probes:
        enabled: true
